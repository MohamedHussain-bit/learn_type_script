// => Array on typeScript
// Syntax Array  const nameVariable : types[] = [valueType , valueType];

// Array type String
const stringArray : string[] = ["medo", "soso"];

// Array type number
const numsArray : number[] = [1,2,3,4,5];

// Array type boolean
const booleanArray : boolean[] = [true, false];

module.exports = {
    stringArray,
    numsArray,
    booleanArray,
};